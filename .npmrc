# Expo SDK 57 把 react 釘在 19.2.3，但 expo-router 的 web 相依（vaul/radix）
# 會拉進 react-dom@19.2.8 並要求 react ^19.2.8，造成 npm peer 解析衝突。
# 我們不做 web 版，這個衝突不影響 Android/iOS。
# `npx expo install` 內部本來就忽略此衝突，這裡讓一般 `npm install` 行為一致。
legacy-peer-deps=true
