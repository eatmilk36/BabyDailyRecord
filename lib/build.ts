/**
 * 手動維護的版本標記，顯示在設定頁最下面。
 *
 * 為什麼需要這個：Expo Go 不會自己更新 bundle。你切背景再回來、甚至重開 APP，
 * 跑的都可能還是上一次抓到的那份 JS。於是「我到底看到的是新版還是舊版」
 * 完全沒辦法從畫面上判斷 —— 只能靠數 tab 有幾個、猜某個按鈕在不在。
 *
 * 有這個標記就一眼可判：設定頁底下的字跟這裡不一樣，就是還沒抓到新 bundle。
 *
 * ⚠️ 這是手動更新的，不是自動從 git 讀的（bundle 裡沒有 git，執行期也讀不到）。
 * 每次要讓使用者驗證新版時，改這裡。
 */
export const BUILD_TAG = '2026-08-10 · UI/UX 稽核修正 S1–S6';

/** 這一版帶了什麼，讓使用者可以自己核對有沒有抓到新 bundle。 */
export const BUILD_NOTES = [
  '兩張寶寶卡有【左側粗色條】，深色模式底色明顯一深一淺',
  '喝奶按鈕是奶油色、尿布按鈕是水藍色（不再跟寶寶的識別色同色）',
  '「開始親餵」「開始睡覺」的字變清楚了（原本淡到看不見）',
  '所有選項 chip 有明顯邊框，大便卡「需要注意」那組是警示色邊框',
  '從彈窗刪除紀錄後會出現「復原」',
  '底部選中的那一格整格有淡色圓角底',
];
